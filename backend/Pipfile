[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = "==4.1"
channels = "==4.0"
django-extensions = "==3.2.1"
django-environ = "==0.9.0"
setuptools = "*"
lock = "*"
# Async
celery = "==5.2.7"                           # Async processing
django-celery-results = "==2.4.0"            # Save Celery results in DB
flower = "1.1.0"                             # Async monitoring
# Logging
django-db-logger = "==0.1.12"                # Database logging
# Testing
freezegun = "==1.2.2"
model-bakery = "==1.9.0"
django-taggit = "==3.1.0"
markdown = "==3.4.1"
flake8 = "*"
black = "==21.8b0"
# Utility
django-mysql = "==4.7.1"                     # Django-MySQL extends Django’s built-in MySQL and MariaDB support
mysqlclient = "==2.1.1"

[dev-packages]

[requires]
python_version = "3.8"
